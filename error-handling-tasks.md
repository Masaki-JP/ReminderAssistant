# エラーハンドリングの残作業

## 1. 操作に応じたエラー表示と復旧処理

### 主な対象

- `ContentView.swift`
- `ContentViewModel.swift`

### 現状

- リマインダー作成時は、期限の解釈失敗・作成先リストの不在・保存失敗を作成画面内で区別できている。
- 読み込み失敗と完了状態の更新失敗は、`ContentView`で同じ「エラー」として表示される。
- 完了状態の更新に失敗しても、楽観的に変更した表示がその場で元に戻らない。
- どちらの失敗でも復旧操作が「再読み込み」になっており、失敗内容との対応が分かりにくい。

### 対応案

- `ContentViewModelError`ごとにタイトル、メッセージ、復旧操作を定義する。
- 読み込み失敗では「読み込めませんでした」と再試行を表示する。
- 完了状態の更新失敗では、対象リマインダーの表示を元に戻すか、自動で再読み込みして整合性を回復する。
- 作成先リストが無効な場合は、設定画面での再選択を案内する。

### 完了条件

- 読み込み失敗と完了状態の更新失敗で、表示内容と復旧操作が区別されている。
- 完了状態の更新失敗後に、画面表示と実データが不整合のまま残らない。

## 2. 1件の失敗で無関係な操作までキャンセルしない

### 主な対象

- `ContentViewModel.swift`（`ReminderOperation`を含む）

### 現状

`ContentViewModel.resolveError(_:as:)`は、通常のエラーが発生すると`reminderOperations`内の全操作をキャンセルする。複数の作成・更新・読み込みが並行している場合、失敗した処理と無関係な操作まで中断される可能性がある。

特に完了状態の切り替えでは、楽観的に変更した表示だけが残る可能性がある。

### 対応案

- エラー処理から一律の全操作キャンセルを外す。
- 各操作が自身の識別子を使って、自分自身だけを終了・削除する。
- 権限失効など、全操作を継続できないエラーに限って全操作をキャンセルする。
- データ整合性の回復が必要な場合は、対象表示を戻すか、読み込みを開始する。

### 完了条件

- 1件の保存・更新失敗が、無関係な作成や更新を中断しない。
- 権限失効時には、従来どおり全操作を安全に停止できる。
- 失敗後も表示とEventKit上の状態が一致する。

## 3. データ変換・キャッシュ失敗をログで観測できるようにする

### 主な対象

- `ReminderStore.swift`
- `ReminderStoreCache.swift`
- `EKReminder+RAReminder.swift`

### 現状

- `ReminderStore.fetch()`の`compactMap`で変換できなかったリマインダーが黙って除外される。
- `ReminderStoreCache`では、保存先取得・読み込み・デコード・書き込みなどの失敗理由を確認できない。
- キャッシュはベストエフォートでよいが、一部データが表示されない問題を後から調査する手段がない。

### 対応案

- `Logger`を導入し、変換できず除外したリマインダーの件数を記録する。
- キャッシュの読み込み、デコード、作成、書き込み、属性設定の失敗理由を記録する。
- ファイル未作成、有効期限切れ、バージョン不一致などの正常なキャッシュミスと、実際のエラーを区別する。
- ログにはリマインダーの件名やメモなどの個人情報を含めない。

### 完了条件

- データ変換で除外が発生したことと、その件数をログで確認できる。
- キャッシュ処理の失敗箇所と理由をログで確認できる。
- 通常のキャッシュミスをエラーとして扱わない。
- 個人情報をログへ出力しない。

## 検証の方針

- `FakeReminderStore`で読み込み、作成、完了状態更新を意図的に失敗させる。
- 各エラーで表示内容、復旧操作、表示状態のロールバックを確認する。
- 複数操作の実行中に1件だけ失敗させ、無関係な操作が継続することを確認する。
- キャッシュ破損、期限切れ、バージョン不一致を分けてログを確認する。
