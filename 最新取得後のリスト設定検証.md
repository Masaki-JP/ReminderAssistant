# 最新取得後のリスト設定検証

この変更は、初回表示に使用したキャッシュの内容を基準に、保存済みの表示対象リストやリマインダー作成先が変更される問題へ対処したものです。

キャッシュは画面を早く表示するための一時的なデータとしてのみ使用し、保存済み設定の検証は、リポジトリから最新のリマインダー一覧を取得して画面へ反映した後に行います。

## 問題が発生する条件

この問題は、次の条件がすべて揃った場合に発生します。

1. 表示対象またはリマインダー作成先として、特定のリストが保存されている。
2. 24時間以内の有効なキャッシュが存在する。
3. 保存済みのリストがキャッシュには含まれていない。
4. リポジトリから取得する最新の一覧には、そのリストが存在する。

通常は最新取得のたびに一覧全体がキャッシュへ保存されるため、発生する可能性は低い問題です。ただし、キャッシュ保存の失敗や、リスト構成とキャッシュ更新のタイミングが重なった場合などには、この不一致が起こり得ます。

## 具体例

保存済みの設定が次の状態だったとします。

```text
表示対象: 「仕事」
リマインダー作成先: 「仕事」
```

リポジトリの最新データには、「個人」と「仕事」の2つのリストがあります。

```text
リポジトリの最新データ
├── 個人
└── 仕事
```

一方、古いキャッシュには「個人」しか含まれていないものとします。

```text
キャッシュ
└── 個人
```

変更前は、次の順序で問題が発生していました。

| 順序 | 処理 |
| --- | --- |
| 1 | 保存済み設定から、表示対象と作成先に「仕事」を設定する。 |
| 2 | キャッシュを読み込み、`editableLists`へ「個人」だけを反映する。 |
| 3 | `editableLists`の変更を契機に、保存済み設定を検証する。 |
| 4 | 「仕事」が存在しないと判断し、表示対象を「個人」または「すべて」へ変更する。 |
| 5 | 作成先を`nil`へ変更し、利用できないことを示すエラーを表示する。 |
| 6 | リポジトリから「個人」と「仕事」を取得する。 |
| 7 | 最新一覧には「仕事」があるが、保存済み設定はすでに変更されているため元に戻らない。 |

リマインダーやリストそのものが削除されるわけではありませんが、利用者は表示対象と作成先を設定し直す必要があります。

## 変更前のコード

`loadReminders()`は、初回のみキャッシュを先に`editableLists`へ反映し、その後にリポジトリの最新取得結果を反映します。

```swift
if shouldReadInitialCache,
   let cachedLists = await reminderStoreCache?.fetch() {
    try Task.checkCancellation()
    self?.editableLists = cachedLists
}

let lists = try await fetchedLists
try Task.checkCancellation()
self?.editableLists = lists
await reminderStoreCache?.save(lists)
```

一方、`ContentView`は`editableLists`が変わるたびに、保存済み設定を検証していました。

```swift
.onChange(of: viewModel.editableLists) { _, lists in
    ensureDisplayedList(from: lists)
    ensureReminderDestinationList(from: lists)
}
```

この監視では、`editableLists`へ反映されたデータが、次のどちらであるかを区別できません。

- 画面を早く表示するためのキャッシュ
- リポジトリから取得した最新データ

そのため、古いキャッシュを最新の確定データとして扱い、保存済み設定を変更していました。

## 今回の対処

### リポジトリの取得結果を反映した処理のIDを保持する

`ContentViewModel`へ`lastAppliedRepositoryFetchID`を追加しました。

```swift
/// リポジトリから取得した最新のリマインダー一覧を画面へ反映した処理のID。
private(set) var lastAppliedRepositoryFetchID: UUID? = nil
```

この値は、キャッシュを読み込んだときには変更しません。リポジトリの最新取得に成功し、その結果を`editableLists`へ反映した直後だけ更新します。

```swift
let lists = try await fetchedLists
try Task.checkCancellation()
self?.editableLists = lists
self?.lastAppliedRepositoryFetchID = operationID
await reminderStoreCache?.save(lists)
```

取得に失敗した場合や、`Task.checkCancellation()`でキャンセルを検出した場合は、この代入へ到達しません。

### 設定検証の契機を変更する

`ContentView`では、`editableLists`そのものではなく、`lastAppliedRepositoryFetchID`の変更を監視します。

```swift
.onChange(of: viewModel.lastAppliedRepositoryFetchID) {
    ensureDisplayedList(from: viewModel.editableLists)
    ensureReminderDestinationList(from: viewModel.editableLists)
}
```

これにより、キャッシュが`editableLists`へ反映されても、保存済み設定は検証されません。リポジトリの取得結果が反映された場合だけ、その時点の`editableLists`を使って検証します。

## 変更後の処理

先ほどの例は、変更後には次のように処理されます。

| 順序 | 処理 |
| --- | --- |
| 1 | 保存済み設定から、表示対象と作成先に「仕事」を設定する。 |
| 2 | キャッシュを読み込み、`editableLists`へ「個人」だけを反映する。 |
| 3 | `lastAppliedRepositoryFetchID`は変わらないため、保存済み設定を変更しない。 |
| 4 | リポジトリから「個人」と「仕事」を取得する。 |
| 5 | 最新一覧を`editableLists`へ反映する。 |
| 6 | `lastAppliedRepositoryFetchID`を更新する。 |
| 7 | 最新一覧を基準に設定を検証し、「仕事」が存在するため設定を維持する。 |

リポジトリの最新一覧にも「仕事」が存在しない場合は、その時点で初めて表示対象を変更し、作成先を解除します。

## UUIDを検証の契機にする理由

`editableLists`の変更だけでは、リポジトリの取得成功を必ず検知できません。

例えば、キャッシュと最新取得の内容が同一だった場合、同じ一覧を代入しても`onChange(of: viewModel.editableLists)`が呼ばれない可能性があります。また、空配列を取得した場合も、初期値の空配列と同じであれば変更として検知できません。

取得処理ごとに異なる`operationID`を使用すると、一覧の内容が同一でも、リポジトリからの取得結果を反映した事実を検知できます。

```swift
self?.lastAppliedRepositoryFetchID = operationID
```

新しいUUIDを追加で生成するのではなく、取得処理を管理するために既に作成している`operationID`を使用しています。

## `hasReadInitialCache`を使用しない理由

`hasReadInitialCache`が表すのは、初回キャッシュの読み込みを試行したかどうかです。

```swift
let shouldReadInitialCache = hasReadInitialCache == false
hasReadInitialCache = true
```

この値はリポジトリの取得結果を待つ前に`true`になります。そのため、キャッシュ反映時の設定検証を除外する目的には使用できません。

また、真偽値は最初の一度しか変化しないため、2回目以降の最新取得成功を通知することもできません。外部でリストが削除された場合などに対応するには、最新取得のたびに設定を検証する必要があります。

`hasReadInitialCache`と`lastAppliedRepositoryFetchID`は、次のように役割が異なります。

| プロパティ | 役割 |
| --- | --- |
| `hasReadInitialCache` | 初回キャッシュの読み込みをすでに試行したかを管理する。 |
| `lastAppliedRepositoryFetchID` | リポジトリの取得結果を画面へ反映したことを通知する。 |

## 変更によって防げること

- 古いキャッシュに対象リストがないだけで、保存済みの表示対象が変更されること
- 古いキャッシュに対象リストがないだけで、リマインダー作成先が解除されること
- 実際には利用できる作成先について、利用できないというエラーが表示されること
- 最新取得後に対象リストが見つかっても、元の設定が復元されないこと

## 対象範囲

今回の変更は、保存済みの通常リストの表示対象と、リマインダー作成先の検証タイミングを対象としています。

キャッシュはこれまでどおり先に画面へ反映されるため、起動直後の表示速度は維持されます。取得に失敗またはキャンセルされた場合は、最新状態を確認できていないため保存済み設定を変更しません。

リポジトリから最新一覧を正常に取得し、その一覧にも保存済みの対象リストが存在しない場合は、従来どおり設定を変更して利用者へ案内します。
