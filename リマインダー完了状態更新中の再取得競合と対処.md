# リマインダー完了状態更新中の再取得競合と対処

この変更は、完了状態の保存を待っている間にEventKitの変更通知が届き、保存前の古い状態で画面とキャッシュが上書きされる問題へ対処したものです。

変更操作中は通知を受けても取得を開始せず、すべての変更操作が終わった後に最新状態を一度だけ取得します。

## 問題が表面化する処理

完了ボタンをタップすると、画面上の状態はすぐに切り替わります。一方、EventKitへの保存は、短時間の再タップを取り消しとして扱えるように0.3秒待ってから開始されます。

該当する処理は次の部分です。

```swift
try await Task.sleep(for: .seconds(0.3))
try await self?.reminderRepository.set(
    id: reminder.id,
    completion: completion
)
guard let index = self?.reminderIndex(for: reminder) else { return }
self?.editableLists[index.list].reminders[index.reminder]
    .setIsCompleted(completion)
```

このコード自体が誤っているわけではありません。問題が起き得るのは、`Task.sleep`と`reminderRepository.set`の`await`中に処理が一時停止し、その間にEventKitの変更通知を処理できるためです。

最後の2行は、保存後の状態を画面上の実値として確定する処理であり、競合の原因ではありません。

## 変更前に発生していた問題

従来は、完了ボタンをタップした時点で実行中だった取得を`cancelLoad()`でキャンセルしていました。しかし、`cancelLoad()`が止められるのは、その時点ですでに存在する取得だけです。0.3秒の待機中に新しいEventKit変更通知が届くと、通知処理は新しい取得を開始していました。

```swift
// 変更前の通知処理
guard self?.error == nil else { return }
self?.loadReminders()
```

例えば、次のような順序で表示の巻き戻りが起こります。

| 時刻 | 処理 |
| --- | --- |
| 0.00秒 | 完了ボタンをタップし、画面を完了状態へ切り替える。タップ時点の取得はキャンセルする。 |
| 0.10秒 | 別のEventKit変更通知が届き、通知処理が新しい取得を開始する。 |
| 0.30秒 | 完了状態をEventKitへ保存しようとする。 |
| 0.40秒 | 先に始まった取得が、保存前の未完了状態を返す。 |
| 0.40秒 | 画面とキャッシュが未完了状態で上書きされる。 |
| その後 | 完了状態が保存され、次の再取得で画面が完了状態へ戻る。 |

利用者から見ると、完了へ切り替えたリマインダーが一度未完了へ戻り、その後また完了へ戻るように見えます。また、保存前の状態が一時的にキャッシュへ保存される可能性もあります。

## 今回の対処

今回の変更では、保存、完了状態更新、削除のいずれかが予約または実行中かを、`reminderOperations`から判断します。

```swift
private var hasPendingMutation: Bool {
    reminderOperations.contains { operation in
        switch operation {
        case .save, .toggleCompletion, .delete: true
        case .load: false
        }
    }
}
```

変更操作中に通知を受け取った場合は`loadReminders()`を呼ばず、`shouldReloadAfterMutation`を有効にします。変更操作中でなければ、従来どおりその場で取得します。

```swift
if self.hasPendingMutation == true {
    self.shouldReloadAfterMutation = true
} else {
    self.loadReminders()
}
```

各変更操作の終了時には、終了した操作を`reminderOperations`から取り除きます。再取得が予約されていて、ほかの変更操作が残っていない場合にだけ、予約を解除して`loadReminders()`を実行します。

```swift
private func finishReminderMutation(with operationID: UUID) {
    reminderOperations.remove(with: operationID)
    reloadRemindersAfterMutationIfNeeded()
}

private func reloadRemindersAfterMutationIfNeeded() {
    guard shouldReloadAfterMutation == true else { return }
    guard hasPendingMutation == false else { return }

    shouldReloadAfterMutation = false
    loadReminders()
}
```

## 変更後の処理

同じ例は、変更後には次のように処理されます。

| 時刻 | 処理 |
| --- | --- |
| 0.00秒 | 完了ボタンをタップし、画面を完了状態へ切り替える。完了状態更新を操作一覧へ登録する。 |
| 0.10秒 | EventKit変更通知が届く。 |
| 0.10秒 | 変更操作中なので取得は開始せず、操作終了後の再取得だけを予約する。 |
| 0.30秒 | EventKitへ完了状態を保存する。 |
| 保存完了 | 終了した完了状態更新を操作一覧から取り除く。 |
| その後 | ほかの変更操作がないことを確認し、保存後の最新状態を一度だけ取得する。 |
| 取得完了 | 保存済みの完了状態で画面とキャッシュを更新する。 |

複数の変更操作が同時に存在する場合も、最後の操作が終わるまで再取得を待ちます。そのため、変更途中の状態ではなく、すべての変更を反映した状態で画面とキャッシュを更新できます。

## 変更によって防げること

- 完了状態を変更した直後に、画面が保存前の状態へ一時的に戻ること
- 保存前の古い取得結果がキャッシュへ保存されること
- 複数の変更通知によって、変更操作中に不要な取得が繰り返されること

## 対象範囲

この変更が対象としているのは、変更通知によって保存前の取得が始まる問題です。0.3秒の待機とEventKitへの保存処理は維持し、通知を受けて再取得を開始する側を調整しています。

保存開始後に同じリマインダーを再タップした場合の扱いは、今回の変更には含まれていません。
