# 完了状態切り替えで中断した再取得の再開

この変更は、完了状態の切り替えによって実行中の取得をキャンセルした後、完了状態の切り替え自体も取り消された場合に、最新データの取得が再開されない問題へ対処したものです。

完了状態の切り替えで実際に取得をキャンセルした場合だけ、変更操作終了後の再取得を予約します。これにより、EventKitの変更通知が発生しない場合でも、中断した取得を補うことができます。

## 問題が発生する条件

アプリは起動時などに、キャッシュを先に表示しながらEventKitから最新のリマインダーを取得します。

その取得中に完了ボタンをタップすると、保存前の古い取得結果によって画面が上書きされないように、`requestCompletionToggle(for:)`が`cancelLoad()`を呼び出します。

```swift
reminderOperations.append(
    .toggleCompletion(id: operationID, reminderID: reminder.id, task: task)
)
cancelLoad()
```

完了状態の保存は、反対操作を取り消せるように1.5秒待ってから開始されます。この待機中に同じリマインダーを再度タップすると、完了状態の切り替えもキャンセルされます。

```swift
try await Task.sleep(for: .seconds(1.5))
```

この場合、EventKitへの保存は行われません。そのため、保存に伴うEventKitの変更通知も発生しません。

## 変更前に発生していた問題

変更前の`cancelLoad()`は、取得タスクをキャンセルして操作一覧から取り除くだけでした。

```swift
private func cancelLoad() {
    reminderOperations.removeAll { operation in
        if case .load(_, let loadTask) = operation {
            loadTask.cancel(); return true
        } else {
            return false
        }
    }
}
```

取得を中断したという情報は、その後の処理へ引き継がれていませんでした。

完了状態の切り替えも取り消された場合、タスクのキャンセルはエラー表示の対象にならないため、`shouldReloadAfterMutation`は有効になりません。また、EventKitを更新していないため変更通知も届きません。

その結果、中断した取得を再開する契機がなくなっていました。

例えば、次のような順序で問題が発生します。

| 時刻 | 処理 |
| --- | --- |
| 0.00秒 | キャッシュを表示し、EventKitから最新データの取得を開始する。 |
| 0.10秒 | 完了ボタンをタップする。画面を完了状態へ切り替え、実行中の取得をキャンセルする。 |
| 0.50秒 | 同じリマインダーを再度タップし、完了状態の切り替えを取り消す。画面を未完了状態へ戻す。 |
| その後 | EventKitは更新されないため変更通知が発生せず、最新データの取得も再開されない。 |

利用者には、別の変更通知や画面の再生成が行われるまで、キャッシュに保存された古い一覧が表示され続ける可能性があります。

## 今回の対処

### `cancelLoad()`がキャンセルの有無を返す

`cancelLoad()`を、実行中の取得をキャンセルしたかどうかを`Bool`で返すメソッドへ変更しました。

```swift
@discardableResult
private func cancelLoad() -> Bool {
    var hasCancelledLoad = false

    reminderOperations.removeAll { operation in
        if case .load(_, let loadTask) = operation {
            hasCancelledLoad = true
            loadTask.cancel(); return true
        } else {
            return false
        }
    }

    return hasCancelledLoad
}
```

戻り値は次の意味を持ちます。

| 戻り値 | 意味 |
| --- | --- |
| `true` | 1件以上の取得タスクをキャンセルした。後で代わりの取得が必要。 |
| `false` | キャンセル対象の取得がなかった。今回の変更による再取得は不要。 |

ほかの呼び出し元では戻り値を必要としないため、`@discardableResult`を付けています。

### 取得をキャンセルした場合だけ再取得を予約する

完了状態の切り替えを操作一覧へ追加した後で`cancelLoad()`を呼び、その戻り値が`true`の場合だけ`shouldReloadAfterMutation`を有効にします。

```swift
reminderOperations.append(
    .toggleCompletion(id: operationID, reminderID: reminder.id, task: task)
)

if cancelLoad() == true {
    shouldReloadAfterMutation = true
}
```

完了状態更新のタスクは、成功、失敗、キャンセルのいずれの場合も、`defer`から`finishReminderMutation(with:)`を呼び出します。

```swift
let task = Task { [weak self] in
    defer { self?.finishReminderMutation(with: operationID) }

    // 完了状態更新の処理
}
```

`finishReminderMutation(with:)`は終了した操作を取り除いた後、再取得が予約されているかを確認します。

```swift
private func finishReminderMutation(with operationID: UUID) {
    reminderOperations.remove(with: operationID)
    reloadRemindersAfterMutationIfNeeded()
}

private func reloadRemindersAfterMutationIfNeeded() {
    guard shouldReloadAfterMutation == true else { return }
    guard hasPendingMutation == false else { return }

    shouldReloadAfterMutation = false
    loadReminders()
}
```

ほかの保存、完了状態更新、削除が残っている場合は、それらがすべて終了するまで再取得を待ちます。

## 変更後の処理

同じ例は、変更後には次のように処理されます。

| 時刻 | 処理 |
| --- | --- |
| 0.00秒 | キャッシュを表示し、EventKitから最新データの取得を開始する。 |
| 0.10秒 | 完了ボタンをタップする。画面を完了状態へ切り替え、実行中の取得をキャンセルする。 |
| 0.10秒 | `cancelLoad()`が`true`を返すため、変更操作終了後の再取得を予約する。 |
| 0.50秒 | 同じリマインダーを再度タップし、完了状態の切り替えを取り消す。画面を未完了状態へ戻す。 |
| 操作終了 | `finishReminderMutation(with:)`が呼ばれる。ほかの変更操作がなければ再取得を開始する。 |
| 取得完了 | EventKitの最新状態で画面とキャッシュを更新する。 |

完了状態の切り替えを取り消さず、EventKitへの保存が成功した場合も、最初に中断した取得を補うため、保存完了後に一度だけ最新状態を取得します。

## 不要な再取得を増やさない理由

`shouldReloadAfterMutation`を有効にするのは、`cancelLoad()`が`true`を返した場合だけです。

完了ボタンをタップした時点で取得処理が存在しなければ、今回の変更による再取得予約は行いません。EventKitの変更通知による通常の再取得処理は、これまでどおり動作します。

## 変更によって防げること

- 完了状態の切り替えを取り消した後、最新取得が再開されないこと
- 別の変更通知や画面の再生成まで、古いキャッシュ表示が残り続けること
- 取得を中断した事実が後続処理へ伝わらず、再取得の契機を失うこと

## 対象範囲

今回の変更が対象としているのは、完了状態の切り替えによって実行中の取得をキャンセルした場合です。

完了ボタンをタップした時点で取得処理が存在しない場合や、取得をキャンセルせずに完了状態を切り替えた場合には、今回の変更による追加の再取得は行いません。

また、この変更は、キャッシュの内容を基準に保存済みの表示対象や作成先リストが変更される問題には対応していません。その問題は、最新取得に成功したタイミングだけでリスト設定を検証する別の対応が必要です。
