# リマインダー完了状態保存中の再タップ競合と対処

この変更は、完了状態の保存がすでに始まった後で同じリマインダーを再度タップすると、画面上の操作は取り消されたように見える一方、EventKitへの保存は完了する可能性がある問題へ対処したものです。

保存前の0.3秒間は従来どおり再タップによる取り消しを受け付け、保存を開始してから処理が終了するまでの間だけ、同じリマインダーの完了状態を再度切り替えられないようにします。

## 問題が表面化する処理

完了ボタンをタップすると、画面上の状態はすぐに切り替わります。その後、短時間の再タップを取り消しとして扱えるように0.3秒待ってから、EventKitへの保存を開始します。

変更前の中心的な処理は次の部分です。

```swift
try await Task.sleep(for: .seconds(0.3))
try await self?.reminderRepository.set(
    id: reminder.id,
    completion: completion
)
guard let index = self?.reminderIndex(for: reminder) else { return }
self?.editableLists[index.list].reminders[index.reminder]
    .setIsCompleted(completion)
```

0.3秒の待機中であれば、タスクをキャンセルすることで保存を開始せずに操作を取り消せます。一方、`reminderRepository.set(id:completion:)`の中でEventKitへの同期的な保存が始まった後は、タスクをキャンセルしても、すでに始まった保存自体を取り消せるとは限りません。

Swiftのタスクキャンセルは、実行中の処理を強制的に巻き戻すものではありません。そのため、画面上の状態だけを元に戻しながら、EventKitには最初のタップによる完了状態が保存される可能性がありました。

## 変更前に発生していた問題

変更前の`onToggleCompletion(_:)`は、同じリマインダーの完了状態更新が操作一覧に存在すれば、そのタスクをキャンセルして画面表示を元に戻していました。

```swift
let isPending = hasPendingCompletionToggle(for: reminder)

if isPending == false {
    requestCompletionToggle(for: reminder)
} else {
    cancelCompletionToggle(for: reminder)
}

let newValue = isPending ? reminder.isCompleted : !reminder.isCompleted
editableLists[index.list].reminders[index.reminder]
    .setDisplayedIsCompleted(newValue)
```

この判定では、「0.3秒の待機中」と「EventKitへの保存開始後」のどちらも、同じ`.toggleCompletion`操作として扱われます。そのため、まだ安全に取り消せる状態と、保存を取り消せない可能性がある状態を区別できませんでした。

例えば、次のような順序で画面とEventKitの状態が食い違います。

| 時刻 | 処理 |
| --- | --- |
| 0.00秒 | 完了ボタンをタップし、画面を完了状態へ切り替える。完了状態の保存を予約する。 |
| 0.30秒 | 待機が終了し、EventKitへの完了状態の保存が始まる。 |
| 0.31秒 | 同じリマインダーを再度タップする。更新タスクをキャンセルし、画面を未完了状態へ戻す。 |
| その後 | すでに始まっていたEventKitへの保存が完了し、EventKit上では完了状態になる。 |
| 再取得時 | EventKitの状態が読み込まれ、画面が再び完了状態へ戻る。 |

利用者から見ると、未完了へ戻したはずのリマインダーが、再読み込み後に完了状態へ戻ったように見えます。

## 今回の対処

今回の変更では、EventKitへの完了状態の保存中であるリマインダーのIDを、`completionToggleLockedReminderIDs`で管理します。

```swift
/// 完了状態をリポジトリへ保存中で、再度の切り替えを禁止するリマインダーのID。
private var completionToggleLockedReminderIDs: Set<Reminder.ID> = []
```

完了ボタンがタップされたとき、対象のIDがこの集合に含まれていれば、その操作を受け付けません。

```swift
func onToggleCompletion(_ reminder: Reminder) {
    guard completionToggleLockedReminderIDs.contains(reminder.id) == false,
          let index = reminderIndex(for: reminder),
          editableLists[index.list].reminders[index.reminder]
              .isMarkedForDeletion == false else { return }

    // 完了状態更新の予約・取り消しと、画面表示の切り替え
}
```

ロックは0.3秒の待機が終わった直後、`reminderRepository.set(id:completion:)`を呼び出す前に設定します。保存の成功・失敗にかかわらず解除されるように、`defer`を使用しています。

```swift
try await Task.sleep(for: .seconds(0.3))
self?.completionToggleLockedReminderIDs.insert(reminder.id)
defer { self?.completionToggleLockedReminderIDs.remove(reminder.id) }

try await self?.reminderRepository.set(
    id: reminder.id,
    completion: completion
)
guard let index = self?.reminderIndex(for: reminder) else { return }
self?.editableLists[index.list].reminders[index.reminder]
    .setIsCompleted(completion)
```

ロックの設定から保存後の画面状態の確定まで、途中にロックが解除された区間を作らないため、保存開始直前と画面上の確定処理を同じ禁止区間に含めています。

## 変更後の処理

同じ例は、変更後には次のように処理されます。

| 時刻 | 処理 |
| --- | --- |
| 0.00秒 | 完了ボタンをタップし、画面を完了状態へ切り替える。完了状態の保存を予約する。 |
| 0.00〜0.30秒 | 保存前の待機中。この間の再タップは従来どおり操作の取り消しとして扱う。 |
| 0.30秒 | 対象のリマインダーをロックしてから、EventKitへの保存を開始する。 |
| 0.31秒 | 同じリマインダーが再度タップされても、ロック中なので操作を受け付けない。画面は完了状態のまま維持する。 |
| 保存完了 | 保存済みの値を画面上の実値として確定する。 |
| 処理終了 | `defer`で対象のロックを解除し、以後のタップを受け付ける。 |

待機中と保存中の扱いが、次のように明確に分かれます。

| 状態 | 再タップ時の動作 |
| --- | --- |
| 0.3秒の待機中 | 更新タスクをキャンセルし、最初のタップを取り消す。EventKitへの保存は開始しない。 |
| EventKitへの保存中 | 再タップを無視し、画面表示も変更しない。 |
| 保存処理の終了後 | ロックを解除し、新しい完了状態の切り替えとして受け付ける。 |

## この実装で問題が解決する理由

問題の原因は、保存開始後にも取り消し操作を受け付けていたことです。保存開始後のキャンセルではEventKitへの保存を確実に止められないため、画面だけを元に戻すと状態の食い違いが発生します。

今回の実装は、確実に取り消せる0.3秒の待機中だけ取り消しを許可し、保存を開始した後は画面側の取り消しも行いません。そのため、画面が未完了へ戻ったのにEventKitでは完了している、という不一致を防げます。

## 変更によって防げること

- 保存開始後の再タップによって、画面だけが保存前の状態へ戻ること
- 再読み込み後に、取り消したはずの完了状態へ戻ったように見えること
- キャンセル不能なEventKitの保存結果と画面表示が食い違うこと

## 対象範囲

ロックはリマインダーID単位です。あるリマインダーの完了状態を保存している間も、別のリマインダーは通常どおり操作できます。

また、禁止するのは保存開始後の同じリマインダーに対する完了状態の再切り替えです。0.3秒の待機中に行われた再タップは、従来どおり最初の操作の取り消しとして扱われます。

この変更は「問題1：保存開始後のキャンセルは取り消しにならない」への対処です。変更通知によって保存前の取得が始まり、画面やキャッシュが一時的に巻き戻る「問題2」については、別の再取得制御で対処しています。
